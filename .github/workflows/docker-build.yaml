name: build-docker
on: [push]
jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      # Premier LS
      - run: ls
      # Checkout au repo
      - uses: actions/checkout@v4
        with:
          repository: 'https://github.com/Paum2tair/coursDeDevOps/'


#checkout

#ls
#build
#images
#push
#pas de creds en clair
